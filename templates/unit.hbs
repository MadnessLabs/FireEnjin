import {{pascalCase name}}Input from "./{{camelCase name}}.input";
import {{pascalCase name}}Output from "./{{camelCase name}}.output";

/**
 * {{description}}
 */
export default async function {{camelCase name}}(
  input: {{pascalCase name}}Input
): Promise<{{pascalCase name}}Output> {
  return input;
}
