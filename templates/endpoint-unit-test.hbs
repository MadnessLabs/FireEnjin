import connect from "../../connect";
connect();

import {{camelCase name}} from "./{{camelCase name}}";

describe("{{titleCase name}}", () => {
  it("Should {{description}}", async done => {
    const response = await {{camelCase name}}({ id: "test"});
    console.log(response);
    expect(response).toMatchObject({ id: "test" });
    done();
  });
});