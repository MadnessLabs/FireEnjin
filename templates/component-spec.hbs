import { {{pascalCase name}} } from "./{{dashCase name}}";

describe("{{dashCase name}}", () => {
  it("should build {{pascalCase name}}", () => {
    expect(new {{pascalCase name}}()).toBeTruthy();
  });
});
